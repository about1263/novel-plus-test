# 用户评论管理测试数据
# 对应 TestUserComment 测试类

test_cases:
  # 分页查询用户书评接口测试
  list_comment_by_page:
    id: "TR_USER_025"
    title: "分页查询用户书评接口测试"
    description: "测试分页查询用户书评接口"
    request:
      method: "GET"
      endpoint: "/user/listCommentByPage"
      params:
        page: 1
        pageSize: 5
    expected:
      code: "200"
      data_exists: true
      data_checks: []
    tags: ["user", "comment", "auth", "smoke"]
    auth_required: true